# Widget Themes

`worldcup_widget.py` keeps the same palette table as the Windows version.
`Midnight` is active:

```python
ACTIVE_THEME = "Midnight"
```

`Aurora` is a recolor-only option. `Broadcast` and `Ticker` palettes are
preserved, but they still need layout-specific rendering work before they are
complete themes.
